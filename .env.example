# Frontend Environment Variables
# Copy this file to .env and update the values

# Backend API URL (for development)
VITE_API_BASE_URL=http://localhost:3001

# For production deployment
# VITE_API_BASE_URL=https://your-backend-api-domain.com

# Solana Network (optional)
VITE_SOLANA_NETWORK=mainnet-beta

# NOTE: DO NOT put any API keys here with VITE_ prefix
# They will be exposed in the frontend bundle!
# All API keys should be in the backend .env file
